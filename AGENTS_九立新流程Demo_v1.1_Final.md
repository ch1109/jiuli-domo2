# AGENTS.md

> 九立报关单证智能核对新流程 Demo · 工程执行约束 v1.1 Final
>
> 本文件服务于 Codex / AI Coding Agent。业务规则以《九立报关单证智能核对新流程 · 产品需求文档 v1.1 Final》和《九立新流程 · 3天高拟真 Demo 需求文档 v1.1 Final》为准。

---

# 1. 项目使命

本项目是 3 天高拟真 Demo，不是正式生产系统。

目标是让业务人员真实操作并确认：

- 客户级查货商品池；
- 入仓号逻辑查货单；
- 查货商品合并；
- 委托书草稿单；
- 商品行匹配；
- 后补查货增量核对；
- 委托资料更新原草稿；
- 人工改值 / 改配；
- 整单人工确认；
- 最终 25 列核对单；
- 查货商品核销。

核心原则：

> **智能能力可以模拟，业务行为必须真实。**

允许模拟：OCR、大模型、Prompt、九立 API、数据库、文件解析、消息队列、正式鉴权、正式存储。

不允许伪造成静态页面：商品池、逻辑查货单、商品合并、草稿、匹配关系、占用 / 释放 / 核销、增量更新、版本、人工修改、人工改配、最终确认、最终核对单、操作历史。

---

# 2. 规则优先级

发生冲突时按以下顺序执行：

1. 用户最新明确要求；
2. 产品需求文档 v1.1 Final；
3. Demo 需求文档 v1.1 Final；
4. 本 AGENTS.md；
5. 现有代码行为。

旧版 v1.0、会前确认 HTML、历史讨论稿仅作为决策过程参考，不得覆盖 Final 规则。

---

# 3. 技术栈

除非用户明确要求，不主动替换：

- React 19；
- Next.js 16；
- TypeScript；
- Tailwind CSS；
- shadcn/ui；
- Zustand；
- TanStack Query；
- Vitest；
- Playwright。

Demo 不建设正式后端。业务状态优先放在清晰可测试的前端业务层，不为了“像正式系统”增加无必要基础设施。

---

# 4. 业务概念必须使用中文语义

面向业务的代码注释、页面状态、演示文案和文档统一使用：

- 待核对；
- 部分核对；
- 可提交人工确认；
- 人工确认中；
- 已完成；
- 暂无查货依据；
- 已找到查货依据；
- 待人工处理；
- 人工已确认；
- 可匹配；
- 草稿占用；
- 已核销。

不要把内部英文枚举直接展示给业务人员。

代码内部如确实需要枚举，可自行使用稳定标识，但必须在业务层集中映射，不允许页面散落硬编码。

---

# 5. 客户是最高隔离边界

- 查货上传时必须明确客户；
- 委托优先从材料识别客户；
- 识别失败必须提示用户补充客户名；
- 客户未确定前不得执行商品匹配；
- A 客户商品绝不能进入 B 客户委托候选。

系统内部使用稳定客户标识，客户名称只用于识别和展示。

---

# 6. PDF 与逻辑查货单严格分离

查货单以入仓号为逻辑边界。

```text
1 PDF + N 个入仓号
=
N 个逻辑查货单
```

不得直接把 PDF 文件编号当逻辑查货单编号。

不得为了匹配委托再次拆分一个入仓号逻辑查货单。

---

# 7. 查货原始商品与查货合并商品必须分层

## 原始商品行

来源事实最小单元，永久保留。

## 合并商品

同一个逻辑查货单内：

```text
品牌相同
+
型号相同
+
产地相同
```

允许逻辑合并。

合并后：

- 数量相加；
- 净重相加；
- 毛重相加。

其他字段不一致时不得静默覆盖。

**禁止为了合并展示而删除原始商品行。**

建立匹配时必须记录实际使用的原始组成行。未被当前草稿使用的原始行继续保持可匹配。

本期不自动把同一条原始商品行按数量拆分成多个份额分给不同委托。

---

# 8. 委托侧商品行不得自行合并

一份委托书及其发票 / 箱单共同形成一张草稿。

多辅助材料可以合并进同一草稿，但委托商品原始行结构保持不变。

---

# 9. 委托资料允许更新原草稿

尚未完成的草稿允许被委托资料更新。

更新必须明确目标草稿：

- 上游传入草稿编号；或
- 用户从当前草稿主动发起更新。

禁止仅凭客户相同、文件名相似或商品相似自动把新材料并入旧草稿。

更新原则：

```text
最新版草稿
+
本次变化材料
+
当前客户商品池
→
只重算受影响商品行
```

稳定行继承。

已完成草稿不得自动更新。

---

# 10. 商品匹配成功后的业务行为

AI 找到可靠查货依据时必须真实执行：

1. 建立商品匹配关系；
2. 更新委托书草稿；
3. 标记本次 AI 更新；
4. 对实际使用的查货原始商品形成草稿占用；
5. 如业务结果变化则生成新草稿版本；
6. 写入操作记录。

此时只是“AI 核对一致 / 已找到查货依据”，不等于人工最终确认。

---

# 11. 三套状态必须保持一致

## 委托书草稿

- 待核对；
- 部分核对；
- 可提交人工确认；
- 人工确认中；
- 已完成。

## 委托商品行

- 暂无查货依据；
- 已找到查货依据；
- 待人工处理；
- 人工已确认。

## 查货原始商品

- 可匹配；
- 草稿占用；
- 已核销。

状态只能通过业务动作统一修改，页面组件禁止直接改复杂业务状态。

---

# 12. 整单人工确认必须拆成两个动作

## 提交整单人工确认

条件：

> 每一条委托商品行都存在对应查货依据。

提交后进入“人工确认中”。

黄色问题可以存在，因为人工需要在该阶段处理。

## 确认完成

确认完成前必须解决：

- 必填缺失；
- 多候选未选择；
- 字段冲突未处理；
- 商品关系不确定。

确认后必须同时：

- 草稿变为已完成；
- 有效商品关系锁定；
- 本次使用的查货原始商品变为已核销；
- 生成最终核对单；
- 后续材料不再自动修改该结果。

---

# 13. 最终结果固定为 25 列

最终草稿和最终核对单严格采用《委托书草稿单模版.xlsx》的字段顺序：

1. 客户名；
2. 品牌；
3. 型号；
4. 商品描述；
5. 品名；
6. 产地；
7. 单位；
8. 数量；
9. 报关单价；
10. 总价；
11. 币种；
12. 件数；
13. 净重；
14. 毛重；
15. sku；
16. 对应的采购；
17. 供应商号码；
18. 供应商；
19. 期票天数；
20. 采购订单号；
21. 物料号码；
22. 托盘数；
23. 入仓号；
24. 产线；
25. 备注。

合计行只计算：

- 数量；
- 件数；
- 净重；
- 毛重。

任何旧文档里的“最终 20 字段”不得继续进入代码或 UI。

---

# 14. 颜色语义

- 红色：必填缺失；
- 黄色：多候选或冲突；
- 绿色：AI 根据当前证据核对一致；
- 灰色 / 普通：暂无查货依据。

绿色绝不能写成“人工已确认完成”。

暂无查货依据不是错误。

---

# 15. 人工操作必须真实改变状态

至少支持：

- 修改字段；
- 选择候选；
- 人工建立遗漏关系；
- 改配；
- 解绑；
- 查看来源；
- 查看原始组成行；
- 查看版本；
- 查看本次变化；
- 提交整单人工确认；
- 确认完成。

改配必须同时完成：

```text
旧原始商品：草稿占用 → 可匹配
旧关系：失效但历史保留
新原始商品：可匹配 → 草稿占用
建立新关系
更新委托行
形成版本
写入操作记录
```

不能只更新 UI 文本。

---

# 16. 增量核对是核心能力

触发：

- 新查货；
- 查货商品变化；
- 新建委托；
- 明确更新现有委托草稿；
- 委托材料重新识别；
- 人工改配 / 修改导致需要重新计算。

只重新检查：

- 暂无查货依据的行；
- 待人工处理行；
- 本次材料变化直接影响的行；
- 因新证据出现冲突的行。

稳定行无新冲突时必须继承。

禁止重新创建整份草稿模拟增量更新。

---

# 17. 业务动作

业务状态统一通过动作层完成，至少包括：

- 加载演示场景；
- 重置演示；
- 新增查货批次；
- 新建委托草稿；
- 更新委托资料；
- 执行首次匹配；
- 执行增量核对；
- 编辑草稿字段；
- 人工改配；
- 解除匹配；
- 提交人工确认；
- 完成最终确认。

页面组件不得绕过业务动作直接写关键状态。

---

# 18. 操作记录与版本

关键业务动作必须记录操作事件。

至少包括：

- 新增查货；
- 新建委托；
- 更新委托资料；
- AI 匹配；
- 增量核对；
- 人工字段修改；
- 人工改配；
- 解除匹配；
- 草稿版本变化；
- 提交人工确认；
- 最终确认；
- 商品核销。

以下情况至少产生草稿版本：

- 首次生成；
- 新查货导致实际结果变化；
- 委托资料更新导致实际结果变化；
- 人工修改字段；
- 人工改配。

无实际业务变化时可只记录检查事件，不强制生成新版本。

---

# 19. Demo 场景必须是真状态初始化

场景不是独立静态页面。

正确：

```text
重置状态
→ 加载场景数据
→ 系统进入真实业务状态
→ 用户可继续自由操作
```

至少支持：

- 查货先到；
- 委托先到；
- 客户识别失败；
- 单 PDF 多入仓号；
- 查货商品合并；
- 多对一；
- 一对多；
- 多对多；
- 后补查货；
- 委托资料更新；
- 字段冲突；
- 重复材料；
- 多候选。

---

# 20. 测试优先级

三天 Demo 不追求 100% 覆盖率。

优先验证业务不变量：

1. 客户隔离；
2. 入仓号拆逻辑单；
3. 查货合并计算正确且原始行不丢；
4. AI 匹配后正确形成草稿占用；
5. 解绑释放；
6. 改配释放旧商品并占用新商品；
7. 后补查货只增量更新受影响行；
8. 委托资料更新原草稿且稳定行继承；
9. 未全行具备查货依据时不能提交人工确认；
10. 问题未处理完时不能确认完成；
11. 最终确认后核销商品并生成 25 列最终核对单；
12. 已完成草稿不能被后续材料自动更新。

优先为这些状态机和业务动作写 Vitest。

Playwright 至少完整跑通一条主演示链路。

---

# 21. UI 原则

核心工作台不要做成超宽 Excel。

推荐：

```text
左侧：委托商品行
右侧：商品详情 / 25字段 / 查货证据 / 来源 / 原始组成 / 操作历史
```

业务人员应该无需重新阅读所有原始材料，就能快速判断：

- 哪些有查货依据；
- 哪些没有；
- 哪些有问题；
- AI 改了什么；
- 当前值从哪里来；
- 当前匹配用了哪些查货原始行；
- 下一步要做什么。

---

# 22. 禁止事项

除非用户明确要求，不得：

- 接真实 OCR / 大模型；
- 建正式数据库；
- 做复杂后端；
- 构建正式鉴权；
- 把旧 20 字段继续用于 UI；
- 把英文内部状态代码直接展示给业务用户；
- 丢弃查货原始商品行；
- 通过静态页面跳转伪造状态变化；
- 为赶 UI 绕过业务动作层；
- 自行发明缺失的业务规则；
- 因为旧代码已经存在就默认旧代码正确。

---

# 23. 不确定规则处理

如果开发中发现 Final 文档仍未定义的业务边界：

1. 不自行决定；
2. 采用可配置设计；
3. 页面保留可讨论状态；
4. 写入 `OPEN_QUESTIONS.md`；
5. 汇报给用户确认。

原则：

> **宁可暴露未知，也不要把猜测变成产品规则。**

---

# 24. 每轮 Codex 工作方式

```text
阅读 Final 文档
→ 理解当前任务
→ 检查现有实现
→ 做最小必要修改
→ 自测业务不变量
→ 汇报修改文件、业务能力、验证方式、剩余限制
→ 停止
```

未经用户要求不得自动扩大下一阶段工作范围。

---

# 25. 最终完成标准

这个 Demo 成功的标准不是代码量，而是：

> **业务人员能够通过实际操作，自然理解客户隔离、查货商品池、查货合并、委托草稿、AI 更新、增量核对、人工改配、整单人工确认、25列最终结果和最终核销，并且关键状态在页面间真实联动。**
