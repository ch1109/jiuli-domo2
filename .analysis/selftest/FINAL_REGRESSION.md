# 并行修改收尾与最终回归

2026-09-20。用户确认另一任务已暂停后，本任务统一收尾。本文替代 ROUND2_REPORT.md 中“暂停、全量失败”的当前状态，原文件保留作为历史记录。

## 统一结果

- 自动候选恢复精确型号规则，不把去后缀推断作为通用 AI 判断。真实 SC-08 仍严格消费已保存的 P3/P4。
- 人工选择允许同客户、已接入、未占用的原始查货行；撤销误加的“必须属于自动候选”限制。型号差异保留原值和冲突，人工处理后才允许完成。
- 保留第二轮核销完整性校验、修改型号/品牌前先解绑保护和逐字段资料修订入口。
- 没有删除失败用例：调整两项候选断言以符合最终约束，人工选择后的冲突断言仍保留。四个场景的冲突失败通过修复人工入口解决，没有改写材料或模型结果。
- 本轮未改动最终 Prompt、模型 request/response 和校准 fixture。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| pnpm test | 15 文件、135 项通过 |
| pnpm exec playwright test --config .analysis/playwright.calibration.config.ts | 38 项通过，无跳过 |
| pnpm typecheck | 通过 |
| pnpm lint | 通过 |
| pnpm build | 通过 |
| verify_sol_execution.py | 宿主模型、阶段顺序及 8 份原始请求响应一致 |
| build_real_calibration_fixture.py --check | 阶段哈希、引用与 fixture 一致 |
| 验收徽标更新后的 demo-store 专项复查 | 39 项通过 |

## 验收范围

验收表与运行时控制台已统一为 17 项关键流程通过。新增记录 SC-03、04、11、20，各自实际验证范围在 spec_final/DEMO_ACCEPTANCE.md 中注明。

SC-03 使用预置组合 PDF 变体；SC-20 页面验证预置重复材料拦截，实际同哈希/同内容去重由已有领域用例覆盖。不能扩大为任意现场文件均已验收。

SC-05、18、19 的原始行分配子流程已通过，仍缺其他型号查货，不能整单完成。SC-17 的完整人工数量/重量/产地判断流程仍未验收。本次没有伪造缺失材料或模型结论。

只有 SC-08 是真实 GPT-5.6 Sol P1→P2→P3→P4 校准场景。其余旧样例的人工路径可操作，不代表它们已分别执行新的模型链路。
