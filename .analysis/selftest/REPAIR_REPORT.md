# 自测阻断修复记录

日期：2026-09-20。承接 `REPORT.md`，本文件记录随后修复的最新结果。

## 本轮结果

修复并独立验证 SC-01、SC-02、SC-06、SC-14、SC-15、SC-16、SC-21 的页面阻断。
SC-05、SC-18、SC-19 的配方指定原始行分配也已通过页面验证，但仍不具备整单完成所需的全部材料。

## 具体修复

- 新建 / 打开草稿：按当前场景配方找到尚未创建的委托，使用已有事实行创建草稿、初始版本和操作记录。保留稳定行 ID、客户待补状态和来源位置；多草稿场景可以依次创建，重复操作不生成重复草稿。
- 材料接入：基线委托可从对应材料行直接生成草稿，不再只支持本地上传转换结果。
- 型号候选：展示同客户、已接入且可匹配的基础型号候选；查找时仅忽略末尾 `#...` 业务标记，原始型号仍保留。AI 自动匹配仍要求原始型号完全一致，后缀差异必须人工选择并处理冲突。
- 人工关系：保留型号等字段差异和原值，用户逐项处理后才能完成。跨客户、已占用素材和已有依据行重复加料均被拒绝；改配后撤销旧确认。
- 字段详情：已处理的冲突不再因为历史证据存在而持续显示为当前冲突；历史来源仍保留。
- Demo 控制台与验收表：补入本轮实际通过的 7 项，避免页面和文档验收口径不一致。

## 新增验证

`tests/scenario-repairs.test.ts`：9 项业务测试，覆盖建稿和去重、客户隔离、人工选择、差异保留、提交/完成门槛、改配撤销确认、重复加料保护以及最终输出。

`e2e/scenario-repairs.spec.ts`：11 项浏览器测试，覆盖 7 个原阻断场景、3 个局部分配场景和材料队列直接建稿。通过页面点击执行动作，仅从持久化状态读取断言，不写入模拟成功状态。

SC-14/15/16/21 均实际执行：首次匹配无关系 → 人工逐行选料 → 不完整关系无法提交 → 全部有依据后提交 → 未处理问题时无法完成 → 逐项保留委托值 → 逐行确认 → 最终单 → 刷新后结果保留。最终合计验证：数量 28135、件数 2、净重 8.0、毛重 9.2；输出列名及顺序严格等于 25 列契约。

SC-05/18/19 按已有场景配方人工选择原始行，验证不同草稿之间不重复占用，以及缺少其他型号依据时禁止提交；没有人为补造缺料。

过程中修正两处测试适配问题：新增建稿按钮后旧导航选择器需精确匹配；最终净重字符串保留来源小数精度，应为 8.0。测试失败没有跳过。

## 检查结果

- `pnpm test`：15 个测试文件，135 项通过。
- `pnpm exec playwright test --config .analysis/playwright.calibration.config.ts --workers=1`：38 项通过，无跳过。
- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `python3 demo-generated/scripts/verify_sol_execution.py`：通过。
- `python3 demo-generated/scripts/build_real_calibration_fixture.py --check`：通过。
- `pnpm build`：通过，Next.js 16.3.5 生产构建完成。

## 边界与剩余事项

- 只有 SC-08 接入真实 GPT-5.6 Sol P1→P2→P3→P4 校准数据。其请求、响应和确定性 fixture 未修改，本轮没有重新调用模型。其他场景仍是明确标识的原有样例/变体及用户人工操作，不宣称得到新增 AI 结论。
- SC-05、SC-18、SC-19：原始行分配子流程已通过；其他型号查货仍缺失，整单验收继续保留材料阻断。
- SC-17：仍需针对实际数量、重量和产地执行完整人工组合与冲突处理验收；本轮未宣称完成。
- SC-03、SC-04、SC-11、SC-20：尚未完成各自完整配方专项验收，不能仅凭已有通用测试标通过。
- 已匹配后修改型号/品牌的关系失效策略、最终关系所有权的全面校验等上轮报告所列边界仍未在本轮完整解决。

现有验收口径：13 项关键流程通过、4 项材料/人工判断限制、4 项未完成专项验收。已知的 7 项页面阻断已经消除，不代表全部 21 项整单验收完成。
