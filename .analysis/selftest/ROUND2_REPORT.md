# 第二轮修复记录（尚未完成整体验收）

2026-09-20。

## 已落地修复

1. `lib/domain/finalization.ts`：封版前检查客户、草稿商品行集合、关系 ID、关系归属、原始查货行存在性、客户一致性、占用状态及占用商品行；拒绝重复引用和额外关系。全部检查成功后才返回核销与最终单结果。
2. `lib/domain/actions.ts`：已有匹配依据时，不允许直接修改型号或品牌；提示先解绑再修改。同值保留原值以解决冲突仍允许。
3. `app/page.tsx`：字段详情补充逐字段“更新委托资料”入口，SC-11 可按原配方修改产地和商品描述，不再只有型号可修订。
4. 修正旧最终核销测试的输入：草稿 lineIds 必须实际包含待封版行，不能继续用不一致测试数据宣称成功。

## 实际验证

- 新增 `tests/finalization-integrity.test.ts`：10 种异常输入在修复前全部误通过；修复后全部拒绝，并验证没有改动输入。正常封版另有 1 项通过。
- 新增 `e2e/scenario-round2.spec.ts`：5 项通过。覆盖 SC-03 组合 PDF 三个入仓号；SC-04 十二条原始行合并展示；SC-20 已接入/重复材料显示与刷新不增行；SC-11 两个指定字段修订且稳定行不变；SC-08 修改型号前必须解绑且改后不得复用旧模型结果。
- 最后一次专项运行：最终核销 11 项 + 真实 Prompt 回放 12 项，共 23 项通过。
- SC-20 本轮浏览器验证的是预置重复材料拦截和状态持久化；同内容不同文件的实际重复上传另有已有领域测试。不要扩大为全上传链路重复文件验收。

## 并行写入冲突

本轮工作期间发现同一工作目录还有另一个 active Codex 任务（ID `01a0bcda-8d86-7ea1-bd70-f573ca7438fc`）。`actions.ts`、`app/page.tsx`、`tests/actions.test.ts`、场景数据在本轮执行过程中出现其他写入：型号去后缀候选规则被加入，测试预期被改为允许去后缀候选，部分场景事实也发生变化。

本轮按原任务要求曾撤销自动候选路径的去后缀规则，保留同客户人工选择入口；随后其他写入仍继续。13:52 的全量业务测试为 129 通过、6 失败：2 项是其他写入的去后缀候选预期与当前精确匹配不一致，4 项是 SC-14/15/16/21 的型号冲突预期与当时场景数据不一致。

因此暂停修改重叠文件，不更新场景验收表为“全部通过”。需要先结束同目录并行写入，再核对最终业务口径、统一代码/数据/测试并完整重跑。本文件的专项结果不代表当前整个共享目录已稳定或已通过全量检查。

SC-17 的完整人工组合验收、SC-05/18/19 的缺料限制仍未解决。最终 Prompt 及真实 GPT-5.6 Sol 请求/响应未由本轮修改。
