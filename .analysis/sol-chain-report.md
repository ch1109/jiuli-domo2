# SC-08 GPT-5.6 Sol 链路现状

2026-09-20已补齐此前缺失的模型执行证据及实际接入。详见 `demo-generated/real-calibration/SC-08-audit/INTEGRATION_REPORT.md`。

宿主日志证实原执行任务使用 gpt-5.6-sol，顺序读取最终 Prompt 并产生 P1→P2→P3→P4。八份请求/响应能从模型工具调用重建并与当前文件比对。执行形态为单个 Sol 任务，不是四次独立 API 调用；本轮复用已验证的结果，没有重写模型响应。

新转换读取全部四阶段响应。SC-08 的 P1 委托事实、P2 查货事实、P3 关系和 P4 FieldDecision 均进入运行态；匹配不再由后缀拼接规则决定。程序负责格式映射、快照校验、占用、版本、人工确认及核销。

旧版 `.analysis/agent-*.md` 是早期401阶段的检查快照，不代表当前状态，保留用于历史溯源。
