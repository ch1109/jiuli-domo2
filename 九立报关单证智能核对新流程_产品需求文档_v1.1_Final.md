# 九立报关单证智能核对新流程 · 产品需求文档 v1.1 Final

> 最终冻结版 · 2026-09-18
>
> 本文档用于统一产品、业务、算法、前端、后端、Prompt 与 POC 验收口径。后续如旧版方案、会前确认页、Demo 讨论稿与本文档冲突，以本文档为准。

---

## 1. 产品定义

九立新流程不再是“上传一套材料，AI 一次性生成最终结果”，而是一个持续核对、持续更新、最终由人工确认封版的智能核对工作台。

核心链路：

```text
查货材料进入
→ 按客户进入对应查货商品池
→ 委托材料进入并形成委托书草稿单
→ AI 在同一客户的商品池内寻找查货依据
→ 按商品行更新委托书草稿单
→ 新材料或原委托资料更新时继续增量核对
→ 全部委托商品行都有查货依据后，允许提交整单人工确认
→ 人工完成最终检查与修正
→ 生成最终核对单
→ 本次使用的查货商品正式核销
```

产品目标不是取消人工，而是把人工从“找材料、找商品、逐字段比对”转变为“检查 AI 已整理好的对应关系、冲突和最终结果”。

---

# 2. 产品范围

## 2.1 本期必须支持

1. 客户级数据隔离；
2. 查货 PDF 单份或多份接入；
3. 一个 PDF 中存在多个入仓号时，按入仓号形成多个逻辑查货单；
4. 查货商品持续进入客户级商品池；
5. 一次委托任务以一份委托书为主体，可附带发票、箱单等辅助材料；
6. 多份辅助材料共同形成一张委托书草稿单；
7. 委托资料允许更新尚未完成的原草稿；
8. 委托商品原始行不自行合并；
9. 查货商品按“品牌 + 型号 + 产地”进行逻辑合并；
10. 商品行级匹配；
11. 一对一、多对一、一对多、多对多关系；
12. AI 自动核对、冲突提示、多候选提示；
13. 人工修改字段、选择候选、改配、解绑、补建关系；
14. 新查货资料或委托资料更新触发增量核对；
15. 草稿版本与变化记录；
16. 整单人工确认；
17. 最终核对单输出；
18. 查货商品最终核销；
19. 字段、商品关系、来源、人工操作全程可追溯；
20. POC 结果质量、人工耗时、商品匹配效果统计。

## 2.2 本期明确不做

1. 跨客户匹配；
2. 一个委托任务同时接入多份独立委托书并自动拆成多个任务；
3. 为了适配委托书而把一个入仓号逻辑查货单再次拆成多个逻辑单；
4. 已经最终确认完成的历史委托被后续材料自动重新打开；
5. 邮件正文识别和邮件流程管理；
6. 依靠订单号等尚未确认存在的额外接口字段进行预绑定；
7. 生产级权限、高并发、消息队列等基础设施能力。

---

# 3. 核心业务概念

本项目后续统一使用以下中文概念，不再使用多套同义词描述同一对象。

## 3.1 客户

商品匹配的最高级隔离边界。

不同客户的委托商品与查货商品绝不能互相匹配。

系统内部应为每个客户维护稳定的“客户标识”；客户名称用于识别和展示，不应作为长期唯一键。

---

## 3.2 材料批次

一次接口提交或一次页面上传形成一个材料批次。

材料批次只用于记录“哪些文件是本次一起进入系统的”，**不是商品匹配边界，也不是判断同一业务单的依据**。

---

## 3.3 原始文件

业务人员实际上传的文件，例如：

- 委托书；
- 发票；
- 箱单；
- 查货 PDF。

原始文件永久保留来源信息，不因后续商品合并、改配或核销而删除历史记录。

---

## 3.4 逻辑查货单

查货侧以**入仓号**作为一张逻辑查货单的边界。

```text
1 个 PDF
可能包含
多个入仓号
=
多个逻辑查货单
```

一个入仓号形成的逻辑查货单不得为了适配某份委托书再次拆成多个逻辑单。

---

## 3.5 查货原始商品行

查货文件中实际识别出的商品明细行。

它是来源追溯、占用、释放和最终核销的最小事实单元。

即使前端为了业务使用把多个查货原始商品行合并展示，原始商品行也不得丢失。

---

## 3.6 查货合并商品

同一个逻辑查货单内，满足以下三项完全相同的查货商品允许合并：

```text
品牌相同
+
型号相同
+
产地相同
```

合并后：

- 数量 = 原始组成行数量之和；
- 净重 = 原始组成行净重之和；
- 毛重 = 原始组成行毛重之和。

其他字段不因为合并而静默覆盖：

- 一致则保留一致值；
- 不一致则保留各原始值及来源，标记为待人工确认；
- 所有原始组成行必须可展开查看。

**查货合并商品只是核对和展示层的逻辑汇总，不代表原始查货行被物理删除。**

建立商品匹配关系时，必须进一步记录本次实际使用了哪些原始组成行。不能只记录“委托商品 ↔ 合并商品”。同一合并商品包含多条原始组成行时，允许其中一部分原始行被某张委托草稿占用，未被使用的原始行继续保持可匹配。

---

## 3.7 委托书草稿单

系统根据一份委托书及其辅助材料形成的、在最终人工确认前持续存在的业务主对象。

草稿单可以被：

- AI 自动核对更新；
- 新查货资料增量更新；
- 委托资料补充或更新；
- 人工修改；
- 人工改配。

最终确认前始终称为“委托书草稿单”。

---

## 3.8 委托商品行

委托书草稿单中的商品明细行。

委托侧严格保留原始商品行结构，不因为型号相同而自行合并。

---

## 3.9 商品匹配关系

系统记录的商品行对应关系：

```text
委托商品行
↕
查货合并商品 / 对应原始查货商品行
```

必须能够继续追溯到：

- 逻辑查货单；
- 入仓号；
- 原始查货商品行；
- 原始文件及所在位置。

---

## 3.10 草稿占用

AI 为某个委托商品找到可靠查货依据后，对对应查货商品形成的**临时使用关系**。

草稿占用的含义是：

- 当前查货商品已被某一尚未完成的委托草稿引用；
- 仍保留在商品池中；
- 不代表人工最终确认；
- 可通过人工解绑或改配释放；
- 默认不得再次被其他活跃草稿重复使用同一原始组成行。

---

## 3.11 正式核销

人工完成整单最终确认后，本次真正使用的查货原始商品行退出后续可匹配范围。

正式核销不是物理删除，历史来源、使用关系和操作记录必须永久可追溯。

---

## 3.12 最终核对单

委托书草稿单经过人工最终确认后形成的封版结果。

最终核对单生成后：

- 不再被后续查货资料自动修改；
- 不再被委托资料更新覆盖；
- 对应查货商品正式核销；
- 保留最终字段、来源、匹配关系、人工修改和冲突处理记录。

---

# 4. 客户识别与隔离规则

## 4.1 查货材料

查货材料进入时，由业务人员明确指定客户。

若没有客户信息，不允许把查货商品放入任意客户商品池。

## 4.2 委托材料

优先从委托书中识别客户名称并映射到系统内部客户标识。

如果识别失败或无法唯一确定：

```text
停止商品匹配
↓
提示“未识别到客户，请补充客户名”
↓
用户手动填写 / 选择客户
↓
确定客户后再进入对应商品池核对
```

客户未确定前允许形成“待补客户信息”的材料记录，但不得执行跨材料商品匹配。

---

# 5. 材料接入规则

## 5.1 委托侧

一次委托任务：

```text
1 份委托书
+
0~N 份发票
+
0~N 份箱单
```

形成一张委托书草稿单。

规则：

1. 多份发票 / 箱单共同用于同一草稿；
2. 委托商品行不得自行合并；
3. 一份委托书不得为适配查货关系而拆成多份委托；
4. 多源材料必须交叉验证；
5. 明确冲突不得静默覆盖。

## 5.2 查货侧

一次可上传：

```text
1 份 PDF
或
多份 PDF
```

每份 PDF 先识别入仓号，再形成对应逻辑查货单和查货商品。

## 5.3 重复材料

### 文件内容完全一致

若文件内容完全一致，按重复材料处理，不重复识别、不重复形成商品。

### 文件字节完全一致

若文件校验值完全一致，同样按重复材料处理；只保留一份有效材料记录，其余记录为重复来源。

---

# 6. 委托书草稿建立与更新

## 6.1 首次建立

委托材料首次进入：

```text
识别客户
↓
抽取委托原始商品行
↓
整合发票 / 箱单辅助事实
↓
形成草稿第 1 版
↓
进入同客户查货商品池寻找核对依据
```

## 6.2 更新原草稿

**委托资料允许更新尚未最终确认的原草稿。**

为了避免把一份新委托误更新到旧草稿，必须满足以下条件之一：

1. 上游接口明确传入“要更新的草稿编号”；
2. 用户从某张现有草稿中主动执行“补充 / 更新委托资料”。

不得仅根据“客户相同、文件名相似、商品相似”自动判定为更新原草稿。

更新可能包括：

- 补充新的辅助材料；
- 替换错误材料；
- 委托商品内容发生修正；
- 对原材料重新识别。

更新后：

```text
读取最新版草稿
+
本次新增或变化的委托资料
+
当前客户查货商品池
↓
只重新检查受变化影响的商品行
↓
形成新草稿版本
```

未受影响且没有新冲突证据的稳定结果默认继承，不从零重新生成整张草稿。

## 6.3 已完成草稿

已经完成最终确认的委托单封版，不允许再被后续材料自动更新。

如业务需要再次处理，应新建新的委托任务，不修改历史最终核对单。

---

# 7. 查货商品池

每个客户拥有独立查货商品池。

商品池中的查货原始商品行存在三种业务状态：

### 可匹配

尚未被任何活跃委托草稿使用。

### 草稿占用

已被某一活跃委托草稿引用，但尚未完成最终人工确认。

仍在商品池中，可查看占用草稿及对应委托商品行。

### 已核销

对应委托已完成人工最终确认，本商品不再参与后续新匹配。

历史记录仍保留。

---

# 8. 商品匹配与自动核对

## 8.1 匹配范围

第一层强制过滤：

```text
客户必须一致
```

只有同一客户下的委托商品与查货商品才允许进入候选匹配。

## 8.2 匹配粒度

最终必须做到商品行级，而不是只记录“委托文件对应查货文件”。

## 8.3 型号匹配

匹配阶段允许对型号进行宽松比较，例如忽略：

- 大小写差异；
- 无业务含义的空格；
- 全角 / 半角差异；
- 已确认不影响主体型号判断的前后附加字符。

但匹配用归一化不等于输出值被改写。最终草稿中的型号值仍需按字段取值规则保留业务需要的原值或经人工确认后的查货值。

## 8.4 AI 匹配成功后的行为

AI 找到可靠查货依据时：

```text
建立商品匹配关系
↓
更新委托书草稿单
↓
标记本次 AI 更新的商品行 / 字段
↓
对应查货原始商品形成草稿占用
↓
等待人工审核
```

此时：

- 允许系统显示“AI 核对一致”；
- 不等于“人工最终确认”；
- 不执行正式核销；
- 人工仍可修改字段、解绑或改配。

---

# 9. 行关系场景

所有关系统一按“逻辑查货单 → 委托书”描述。

## 9.1 一对一

一张逻辑查货单中的商品只用于一份委托。

## 9.2 多对一

一份委托的商品核对依据分散在多张逻辑查货单中。

## 9.3 一对多

同一张逻辑查货单中的不同原始商品行，先后被不同委托使用。

## 9.4 多对多

多个逻辑查货单中的不同商品行，被多份委托交叉使用。

只有真正存在交叉商品关系才叫多对多；多个互不相干的一对一不属于多对多。

---

# 10. 增量核对机制

以下事件会触发未完成草稿的增量核对：

### 查货侧

- 新增查货 PDF；
- 新增逻辑查货单；
- 查货识别结果发生有效变化；
- 查货商品行发生有效变化。

### 委托侧

- 新建草稿；
- 明确更新现有草稿的委托资料；
- 辅助材料补充；
- 当前草稿材料重新识别；
- 委托商品行发生有效变化。

增量核对原则：

```text
已有稳定结果继承
+
只处理新增证据真正影响的行
```

优先重新检查：

1. 当前没有查货依据的商品行；
2. 存在多个候选的商品行；
3. 存在字段冲突的商品行；
4. 本次材料变化直接影响的商品行；
5. 本次新增证据使原有稳定结果出现新冲突的商品行。

每次有效变化形成新草稿版本，并记录变化原因和前后差异。

如果本次检查没有造成业务结果变化，只记录检查事件，不强制生成新版本。

---

# 11. 草稿与商品状态

## 11.1 委托书草稿状态

### 待核对

当前没有商品完成有效查货核对，或客户信息仍未补齐。

### 部分核对

部分商品已经找到查货依据，仍有商品没有查货依据。

### 可提交人工确认

**所有委托商品行都已经存在对应查货依据。**

这是允许进入整单人工确认的唯一前提。

注意：进入该状态并不代表所有字段都已经无冲突，只表示每一行都已经有可供人工核对的查货证据。

### 人工确认中

业务人员已经进入整单最终检查，对冲突、候选、缺失和 AI 更新结果进行最后确认与修正。

### 已完成

人工完成最终确认，最终核对单已生成，对应查货商品已正式核销。

---

## 11.2 委托商品行状态

### 暂无查货依据

当前客户商品池中还没有可用于该行核对的查货商品。

这不是异常，只表示材料尚未到齐或还未找到可靠匹配。

### 已找到查货依据

AI 已找到可靠商品关系并更新草稿，等待人工最终审核。

### 待人工处理

出现以下任一情况：

- 多个候选；
- 字段冲突；
- 必填字段缺失；
- 商品对应关系不确定。

### 人工已确认

人工已经确认或修正该商品行当前结果。

---

# 12. 颜色与交互语义

颜色只表示当前核对状态，不代表最终业务结论。

### 红色

必填字段缺失。

### 黄色

两种情况：

1. 存在多个候选；
2. 多源或委托与查货之间存在字段冲突。

### 绿色

AI 根据当前证据判断委托值与查货事实核对一致。

绿色的准确含义是：

> **AI 核对一致，等待人工最终确认。**

不得理解为“已经人工确认完成”。

### 普通 / 灰色

暂无对应查货依据，当前尚未形成核对结论。

---

# 13. 整单人工确认

整单人工确认必须拆成两个不同动作，避免概念混淆。

## 13.1 提交整单人工确认

允许条件：

> **委托书中的每一个商品行都已经存在至少一个对应的查货依据。**

达到该条件后，草稿进入“可提交人工确认”。

业务人员点击“提交人工确认”后进入整单最终检查。

此时即使仍存在黄色冲突、候选值或需要修正的字段，也允许进入人工确认，因为这些问题正是最终人工检查需要处理的内容。

## 13.2 完成人工最终确认

在人工确认页面中，业务人员必须完成：

- 检查 AI 已核对一致的商品；
- 处理多候选；
- 处理字段冲突；
- 补齐必须字段；
- 修正错误值；
- 修正错误商品关系。

存在未解决的必填缺失、未选择的候选或未处理冲突时，不允许点击“确认完成”。

确认完成后：

```text
草稿封版
↓
生成最终核对单
↓
本次使用的查货商品正式核销
↓
后续新材料不再自动修改本单
```

---

# 14. 人工操作能力

草稿必须支持人工：

1. 修改字段；
2. 选择候选值；
3. 查看字段来源；
4. 查看匹配查货商品及原始组成行；
5. 人工建立遗漏匹配；
6. 修改匹配关系；
7. 解除错误匹配；
8. 查看本次 AI 更新内容；
9. 查看历史版本；
10. 提交整单人工确认；
11. 完成人工最终确认。

所有人工操作至少记录：

- 操作人；
- 操作时间；
- 操作类型；
- 修改前；
- 修改后；
- 影响的商品行；
- 影响的查货来源。

---

# 15. 草稿版本机制

每一张未完成委托书草稿维护连续版本，例如：

```text
第1版
第2版
第3版
……
```

以下情况产生新版本：

- 首次生成草稿；
- 新查货资料导致实际业务结果变化；
- 委托资料更新导致实际业务结果变化；
- 人工修改字段；
- 人工改配商品关系。

每个版本必须记录：

- 触发原因；
- 本次新增或变化材料；
- 变化商品行；
- 旧值；
- 新值；
- 新增 / 删除 / 修改的商品匹配关系；
- 更新时间；
- 系统自动更新或人工修改。

---

# 16. 最终输出契约

本版本以用户上传的《委托书草稿单模版.xlsx》为最新权威模板。

此前文档中的“最终 20 字段”说法全部废止。

最终草稿单与最终核对单统一采用 **25 列**：

| 序号 | 字段 |
|---:|---|
| 1 | 客户名 |
| 2 | 品牌 |
| 3 | 型号 |
| 4 | 商品描述 |
| 5 | 品名 |
| 6 | 产地 |
| 7 | 单位 |
| 8 | 数量 |
| 9 | 报关单价 |
| 10 | 总价 |
| 11 | 币种 |
| 12 | 件数 |
| 13 | 净重 |
| 14 | 毛重 |
| 15 | sku |
| 16 | 对应的采购 |
| 17 | 供应商号码 |
| 18 | 供应商 |
| 19 | 期票天数 |
| 20 | 采购订单号 |
| 21 | 物料号码 |
| 22 | 托盘数 |
| 23 | 入仓号 |
| 24 | 产线 |
| 25 | 备注 |

## 16.1 字段填写层级

为避免“模板里有这个字段”与“这个字段必须有值”混为一谈，25 个字段按草稿要求分三类：

### 草稿必填

- 客户名；
- 品牌；
- 型号；
- 品名；
- 产地；
- 单位；
- 数量；
- 报关单价；
- 总价；
- 币种；
- 件数；
- 净重；
- 毛重。

缺失时字段标红，并进入人工处理；允许草稿继续存在，但在最终“确认完成”前必须处理。

### 选填

- 商品描述；
- 备注。

为空不视为异常。

### 随模板 / 有则提取

- sku；
- 对应的采购；
- 供应商号码；
- 供应商；
- 期票天数；
- 采购订单号；
- 物料号码；
- 托盘数；
- 入仓号；
- 产线。

这类字段是否出现取决于客户模板和原始材料；没有业务来源时不得为了填满模板而编造值。

## 16.2 合计行

模板末尾保留“合计”行。

当前已确认的合计字段：

- 数量；
- 件数；
- 净重；
- 毛重。

其他字段不因为存在合计行而自动汇总。

## 16.3 草稿与最终结果结构一致

委托书草稿单和最终核对单使用同一套 25 列业务字段。

区别不在字段结构，而在状态：

```text
草稿单
= AI 持续更新 + 可人工修改 + 尚未封版

最终核对单
= 人工确认完成 + 字段和关系锁定 + 历史可追溯
```

## 16.4 字段来源

每个最终字段都必须能够查看：

- 当前值；
- 来源材料；
- 原始值；
- 原始位置；
- 是否经过 AI 更新；
- 是否经过人工修改；
- 是否存在过冲突。

若字段级业务规则没有明确的来源优先级，遇到多源冲突不得静默覆盖，必须保留冲突并交给人工确认。

---

# 17. 查货合并与多委托使用边界

查货合并不得破坏后续一对多 / 多对多关系。

例如：

```text
原始行03  型号 A / 中国台湾 / 品牌 X / 数量50
原始行08  型号 A / 中国台湾 / 品牌 X / 数量30

页面合并展示：
型号 A / 中国台湾 / 品牌 X / 数量80
```

系统仍必须知道 80 来自原始行03和原始行08。

如果后续：

```text
原始行03 → 委托 W001
原始行08 → 委托 W002
```

系统能够分别记录占用和核销，不因为前端曾展示“数量80”而丢失分配依据。

本期不把“同一条原始查货商品行按数量再次拆成多个份额给不同委托”作为默认能力；遇到此类情况应标记为待人工处理，避免在没有明确业务规则时自动分摊数量或重量。

---

# 18. POC 评估方式

新流程不再只看字段准确率。

至少分为三个维度：

## 18.1 业务流程认可

业务人员能否顺畅完成核心场景，并认可工作方式。

## 18.2 结果质量

分开评价：

### 字段判断

重点评价：

- 缺失提示是否正确；
- 候选是否覆盖正确答案；
- 冲突提示是否真实；
- AI 标记“核对一致”后是否仍被人工改错。

AI 错误把问题字段判断成“核对一致”，应作为较严重错误。

### 商品匹配

单独评价：

- AI 推荐关系被人工确认；
- AI 推荐关系被人工改配；
- AI 未推荐但人工补建关系；
- 商品匹配错误是否造成字段错误。

商品关系错误导致的字段连带变化，不应重复计算为两次独立错误。

## 18.3 人工处理耗时

比较传统人工方式与 AI 工作台方式在以下环节的耗时：

- 查找；
- 检查；
- 修改；
- 返工。

等待材料和人员暂时离开不计入人工有效处理时间。

---

# 19. POC 必测场景

1. 一张逻辑查货单 → 一份委托；
2. 多张逻辑查货单 → 一份委托；
3. 一张逻辑查货单 → 先后多份委托；
4. 多张逻辑查货单 ↔ 多份委托；
5. 委托先到、查货后到；
6. 查货先到、委托后到；
7. 一个 PDF 存在多个入仓号；
8. 委托 + 发票 + 箱单存在字段冲突；
9. 重复材料；
10. 后补查货触发原草稿增量核对；
11. 委托资料更新原草稿；
12. 查货同品牌 + 同型号 + 同产地商品合并；
13. 客户识别失败后人工补充客户信息；
14. 多候选人工选择；
15. AI 改配后原查货商品被释放、新商品被占用；
16. 全部商品存在查货依据后提交整单人工确认；
17. 最终确认后生成 25 列最终核对单并核销查货商品。

---

# 20. 产品验收标准

## 客户隔离

任何委托任务只能匹配同一客户商品池。

## 客户识别失败

未识别客户时必须明确提示用户补充客户信息，补充前不得执行商品匹配。

## 入仓号逻辑单

一个 PDF 存在 N 个入仓号时，形成 N 个逻辑查货单。

## 查货合并

同品牌 + 同型号 + 同产地的查货商品能够逻辑合并；数量、净重、毛重正确累加，且原始组成行不丢失。

## 商品关系

任何匹配都能追溯到委托商品行、逻辑查货单和原始查货商品行。

## AI 更新

AI 匹配成功后更新委托书草稿单并明确标记本次变化，但不直接视为人工最终确认。

## 增量更新

新增查货或明确更新委托资料时，只更新受影响商品，不重新生成整张草稿。

## 整单确认

只有所有委托商品行都有对应查货依据时，才允许提交整单人工确认。

## 最终确认

未解决的必填缺失、候选或冲突必须在“确认完成”前处理。

## 最终核销

只有人工最终确认完成后，对应查货原始商品才正式退出后续匹配范围。

## 最终输出

最终核对单必须严格遵循《委托书草稿单模版.xlsx》的 25 列字段顺序，并保留数量、件数、净重、毛重合计。

## 可追溯

所有最终字段、商品关系、AI 更新和人工修改均可回溯。

---

# 21. 最终产品一句话定义

> **九立报关单证智能核对工作台，是一个以客户级查货商品池为事实基础、以委托书草稿单为工作主线，通过商品行级持续匹配和增量核对不断更新草稿，在全部商品具备查货依据后交由人工完成最终确认，并在封版后核销对应查货商品的智能核对系统。**
